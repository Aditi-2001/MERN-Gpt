import React from 'react'

const NotFound = () => {
  return (
    <div style={{
        display: 'flex',
        justifyContent: 'center',
        alignContents: 'center',
        fontSize: '20px'
    }}>
      NotFound
    </div>
  )
}

export default NotFound
